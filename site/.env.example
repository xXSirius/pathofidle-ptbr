# Copie para .env (não versionado) e preencha se quiser.

# Google Tag Manager — opcional. Deixe vazio para não injetar nenhum
# script de rastreamento (padrão deste projeto: sem anúncios, sem GTM).
PUBLIC_GTM_ID=
